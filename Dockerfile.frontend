FROM node:20-alpine

WORKDIR /app
RUN corepack enable && corepack prepare pnpm@9.6.0 --activate

COPY frontend/package.json ./package.json
COPY frontend/pnpm-lock.yaml ./pnpm-lock.yaml || true
RUN pnpm install

COPY frontend .
EXPOSE 3000
CMD ["pnpm", "dev", "--host", "0.0.0.0"]