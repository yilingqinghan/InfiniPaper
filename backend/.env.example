
ENV=dev
SECRET_KEY=change-me

# Database (local, no Docker)
DATABASE_URL=postgresql+psycopg://infinipaper:infinipaper@127.0.0.1:5432/infinipaper

# Redis / Celery (optional locally; you can ignore if not running)
REDIS_URL=redis://127.0.0.1:6379/0

# GROBID (optional; leave blank to disable or point to a running service)
GROBID_URL=http://127.0.0.1:8070

# Embedding
EMBEDDING_MODEL_NAME=specter2
